HMAC_SECRET=change-me-demo-key
AT_USERNAME=sandbox
AT_API_KEY=
PUBLIC_BASE_URL=http://localhost:8000
# Optional — enables the Ask Mavuno (USSD menu 6) AI advisor.
# When unset, the advisor falls back to a deterministic rule-based
# answer keyed to the question and crop, so demos never go silent.
GROQ_API_KEY=
